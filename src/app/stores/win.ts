import Store from '../utils/store';

export const scrollX = new Store(window.scrollX);
export const scrollY = new Store(window.scrollY);

export const innerWidth = new Store(window.innerWidth);
export const innerHeight = new Store(window.innerHeight);
